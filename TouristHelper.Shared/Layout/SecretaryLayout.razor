@inherits LayoutComponentBase
@using TouristHelper.Shared.Services
@inject ChatService Chat

<div class="flex h-screen bg-gray-50">
    <!-- Sidebar -->
    <aside class="w-64">
        <div class="sidebar-header">
            Let's Travel 🌍
            <div class="sidebar-subtitle">Secretary Panel</div>
        </div>

        <nav class="flex-1 px-4 py-6 space-y-2">
            <NavLink href="/secretary/dashboard" class="nav-item" activeClass="nav-item-active">📊 Dashboard</NavLink>
            <NavLink href="/secretary/hotel-bookings" class="nav-item" activeClass="nav-item-active">🏨 Hotel Bookings</NavLink>
            <NavLink href="/secretary/tour-bookings" class="nav-item" activeClass="nav-item-active">🗺️ Tour Bookings</NavLink>
            <NavLink href="/secretary/ride-bookings" class="nav-item" activeClass="nav-item-active">🚗 Ride Bookings</NavLink>
            <NavLink href="/chat" class="nav-item" activeClass="nav-item-active">
                💬 Chat
            </NavLink>
        </nav>

        <div class="px-4 py-4 border-t border-gray-200">
            <button class="logout-button">
                Logout
            </button>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="flex-1">
        @Body
        <ChatToggle />
    </main>
</div>
